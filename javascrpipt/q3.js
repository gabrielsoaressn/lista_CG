console.log("-----  QUICK SORT -----");

function comparaPivo(meuArray)
{
    if(meuArray[0]>)
}
function quickSort(meuArray)
{
    quickSort(comparaPivo(meuArray));// todos os elementos menores que ele ficarão a sua esquerda em um novo array que entre recusrivamente na mesma função.   
}
// pausa para alomoço
